# API

Interactive OpenAPI docs are auto-generated by FastAPI at `/docs` (Swagger
UI) and `/redoc` on any running instance — the authoritative, always-in-
sync reference. This document is a human-readable index; the list below
was extracted directly from the route table (`grep '@app\.' app/main.py`),
not hand-maintained separately from the code.

Endpoints marked 🔒 require `X-API-Key` when `API_KEY` is set in the
environment (no-op in default demo mode — see `docs/SECURITY.md`). When
`DASHBOARD_PASSWORD` is set, every `/api/` route below (reads included)
additionally requires a valid session token or `X-API-Key` — see
Authentication below.

## Authentication

```
GET  /api/auth/status                        {login_required, authenticated} — public, always reachable
POST /api/auth/login                         {password} -> {token, expires_in} — public, rate-limited per IP
```

Send the returned token as `Authorization: Bearer <token>` on subsequent
requests. No-op (both endpoints report open/authenticated) unless
`DASHBOARD_PASSWORD` is set. See `app/auth.py`.

## Dashboard & analytics

```
GET  /api/dashboard                          executive metrics; ?merchant_id= to scope
GET  /api/analytics                          segment + automation breakdown; ?merchant_id=
GET  /api/analytics/strategy-performance     Recovery Policy Optimizer stats
GET  /api/audit                              global audit feed; ?case_id= to scope
GET  /api/models/current                     current ML model version + real metrics
GET  /api/integrations/status                live provider/auth/DB configuration state
```

## Recovery cases

```
GET  /api/recovery-cases                          list; ?status= ?source_type= ?merchant_id= ?prioritized=true
GET  /api/recovery-cases/export.csv               CSV export, same filters
GET  /api/recovery-cases/{id}                      full detail incl. audit trail, ML prediction, expected value
POST /api/recovery-cases/{id}/analyze          🔒  run diagnosis
POST /api/recovery-cases/{id}/execute          🔒  run next bounded action
POST /api/recovery-cases/{id}/approve          🔒  human approves an escalated case
POST /api/recovery-cases/{id}/reject           🔒  human closes case without further action
POST /api/recovery-cases/{id}/send-payment-link 🔒  manually trigger a payment link (real Razorpay or mock)
POST /api/recovery-cases/{id}/promise-to-pay   🔒  record a promise-to-pay
```

## Promises to pay

```
POST /api/promises/{id}/fulfill  🔒  marks fulfilled -> case RECOVERED
POST /api/promises/{id}/break    🔒  marks broken -> stronger action / escalation
```

## Webhooks (spec section 7, 22)

```
POST /api/webhooks/payment    accepts our simplified shape OR a real Razorpay payment-event payload
POST /api/webhooks/checkout   checkout-abandonment event
POST /api/webhooks/invoice    overdue-invoice event
POST /api/events              generic ingestion, routes by event_type prefix
```

All four accept a signature via `X-Razorpay-Signature` or the generic
`X-Webhook-Signature` header, verified when `PAYMENT_WEBHOOK_SECRET` is
set — see `docs/RAZORPAY_INTEGRATION.md` section 5.

## Merchants (multi-tenant)

```
GET  /api/merchants   list with live per-merchant customer/case counts
```

## Outbound merchant webhooks

Lets a merchant's own systems subscribe to recovery events instead of
polling. Payloads are POSTed as JSON, signed with
`X-RecoveryOS-Signature: HMAC-SHA256(secret, body)` — the same scheme
`app/security.py` requires of Razorpay's inbound webhooks to us. See
`app/outbound_webhooks.py`.

```
GET    /api/merchants/{merchant_id}/webhooks                       list subscriptions (secret never re-shown)
POST   /api/merchants/{merchant_id}/webhooks                   🔒  create — {url, event_types?} -> {id, secret} (shown once)
DELETE /api/merchants/{merchant_id}/webhooks/{webhook_id}      🔒  remove a subscription
GET    /api/merchants/{merchant_id}/webhooks/{webhook_id}/deliveries   last 50 delivery attempts (success/status/error)
```

`event_types` (default: all four) is any subset of `case.opened`,
`case.recovered`, `case.escalated`, `case.stopped`.

## Customers, invoices, payments

```
GET  /api/customers/{id}
GET  /api/invoices     ?status=
GET  /api/payments     ?status=
```

## Policies

```
GET  /api/policies                     active policy (operator-edited if saved, else default)
PUT  /api/policies                 🔒  persist new limits — takes effect on the next policy check
```

## Simulation / demo

```
POST /api/simulation/run           n_customers= (default 60, capped at 2000)
POST /api/simulation/reset     🔒  clears all demo data
POST /api/simulation/replay-best   replays the hero case end-to-end
GET  /api/simulation/hero-case     fetch the hero case's current state
```

## What's NOT versioned (`/api/v1/...`) — honest deviation from spec section 42

Endpoints are under `/api/...`, not `/api/v1/...`. For a 3-day build with
no external API consumers yet, versioning the URL prefix added no real
value and was deprioritized; adding a `/v1/` prefix later is a mechanical
change (a router mount, not a rewrite) whenever an actual breaking change
needs to coexist with an old version.
